\section{Conclusion}
% OUTLINE -- draft next. No prose yet.
% - Restate the deliverable: a horizon-wide safety certificate for closed-loop process operation,
%   obtained by lifting the per-cycle composed certificate \citep{busico_m4} with adaptive conformal
%   inference \citep{gibbs2021}.
% - The four results (R1-R4): constrained loop holds the floor; blind loop is in-spec yet unsafe;
%   validity invariant to deadtime; adaptive calibration finite where Bonferroni is vacuous.
% - The lift from per-cycle to horizon is the bridge from a certified component stack to a certified
%   operating campaign.
% - Future: plantwide eps-budget (modular risk allocation) and real-plant validation on an operating
%   LPG/petrochemical column.
