\section{Related work}
% OUTLINE -- draft next. No prose yet.
% (a) Conformal prediction and distribution-free UQ: foundations \citep{vovk2005, shafer2008},
%     regression \citep{lei2018}, quantile variant \citep{romano2019}, overview \citep{angelopoulos2023}.
% (b) Validity without exchangeability: covariate shift \citep{tibshirani2019} and the
%     beyond-exchangeability framework \citep{barber2023} -- the basis for coverage under the
%     dependence that closed-loop feedback induces.
% (c) Online / adaptive conformal under distribution shift: ACI \citep{gibbs2021} -- the engine
%     that delivers a long-run coverage guarantee from delayed, dependent residuals.
% (d) Real-time optimisation and modifier adaptation \citep{marchetti2009, chachuat2009};
%     plantwide / self-optimizing control \citep{skogestad2000} for the closed-loop framing.
% (e) The IPIS series this paper completes: soft sensing \citep{busico_m1}, similarity-calibrated
%     prognostics \citep{busico_m2}, conditionally calibrated conformal RTO \citep{busico_m3}, and
%     the per-cycle composed certificate \citep{busico_m4}.
% Gap statement: none of the above delivers a HORIZON-wide guarantee on a JOINT (quality AND
%   equipment-health) safety event in a DYNAMIC closed loop with DELAYED labels. That is R1-R4.
