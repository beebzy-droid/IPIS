# Lean runtime for the soft-sensor serving container (Phase 1D.2d).
# Only what the FastAPI app actually imports — NOT the full project dependency set
# (no torch / xgboost / mlflow / streamlit / coolprop / gekko / asyncua ...).
# The empirically-confirmed serving import graph is: fastapi, uvicorn, pydantic,
# numpy, scipy, pandas, scikit-learn, river, joblib.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
numpy>=1.26
scipy>=1.12
pandas>=2.1
scikit-learn>=1.4
river>=0.21
joblib>=1.3
