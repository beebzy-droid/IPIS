# Claims-to-evidence table (Phase 1F.1)

Audit rule: a claim enters the paper only if this table maps it to a number in
`docs/module1/results.md`, a runnable artifact, and (where visual) a figure with a
source script. **Gap** column = what must exist before the claim is submittable.

Status legend: ✅ evidence + artifact in repo | 🟡 evidence in repo, figure/script
missing | 🔴 claim needs new measurement or must be cut.

| # | Claim (paper wording, condensed) | Evidence (numbers) | Artifact | Figure (planned) | ADR | Status / gap |
|---|---|---|---|---|---|---|
| C1 | Blocked time-series CV + one-SE makes soft-sensor model selection reproducible where adjacent validation misleads | Debutanizer: adjacent CV = cross-regime lottery → blocked+one-SE reproducible selection (results §1A). SECOM: CV R² explodes to −7×10⁴ at weak shrinkage; one-SE contains damage at α*=10, 6/441 coefs (results §1E) | `evaluation/blocked_cv.py`, `scripts/secom_baseline.py` | F3: CV-path plot, mean±SE vs α (Debutanizer + SECOM panels) | 007, 012 | 🟡 figure script for the Debutanizer panel (SECOM numbers print already) |
| C2 | Thermodynamically grounded features outperform raw-signal models in robustness | Debutanizer: bubble-point C4, α(T), stripping factor α×reflux beat linear-on-raw across regimes (results §1A) | `features/*physics*` | F2: feature-ablation bar chart per regime | 007 | 🟡 ablation numbers exist; figure script missing |
| C3 | Transport-lag diagnosis must precede model judgment (lag-zero failure is diagnostic, not a physics failure) | u5 @ lag-15 is the stable carrier, r² ≈ 0.51+ vs ~0 at lag 0 (results §1A) | `features/tep_physics_features.py::diagnose_transport_lag` | F2 inset or table T1 | 007 | ✅ (table form suffices) |
| C4 | Open-loop bias-update (Shardt) absorbs drift/offset under delayed, infrequent labels | TEP: corrected vs raw R² per mode (results §1B). SECOM: R² −1.84 → −0.16 (θ=2), pure-offset absorption (results §1E) | `evaluation/bias_update.py` | F4: residual trace raw vs corrected around a drift event | 008 | 🟡 trace figure script missing |
| C5 | Drift alarming on *corrected* residuals flags only what the bias-update cannot absorb | 1B design + serving default `drift_on="corrected"` (results §1B, ADR-011 §) | `evaluation/drift.py`, `serving/service.py` | — (textual) | 008, 011 | ✅ |
| C6 | (a) The selection/drift methodology transfers across process topology unchanged (Debutanizer recipe rebuilt on TEP, same calibration-drift signature); (b) Yan functional SBC migration is **10.0× more data-efficient** than from-scratch in within-TEP regime migration on BOTH targets (two-layer = migration + bias-update 0.3,2; n_repeats=8; 90% of ceiling at 5% vs 50% target data; GP coverage 92–98%, wider for the larger mode3 gap); OSBC 1.0×/4.0×; Luo 1.0×/1.0× — the degeneracy is empirically exact (≡ from-scratch to ±0.001–0.006 at every fraction); WITHOUT the bias-update layer migrated curves cap below 90%-of-bar (mode2 n/a, mode3 3.3×) — the two-layer composition is load-bearing; literal cross-process parameter migration is mathematically inapplicable (disjoint input spaces) | FROZEN: `docs/paper/evidence/efficiency_tep.json` (argv-stamped); results §1C; ADR-009 | `migration/*`, 1C scripts | F5: data-efficiency curves (rendered from frozen evidence) | 009 | ✅ frozen + rendered |
| C7 | Static split conformal is invalid under drift/regime change; ACI restores regime-uniform validity | Synthetic: split collapses 0.403 post-drift, ACI 0.899. Real TEP: split swings 0.847–0.957 across modes; corrected+ACI **0.90 ± 0.003** uniform (modes 1–3 × θ∈{2,5}); EnbPI 0.857–0.897 (results §1D.1/1b) | `evaluation/conformal.py`, `scripts/conformal_eval.py`, `scripts/conformal_synthetic_check.py` | F6: synthetic collapse panel (exists: `conformal_synthetic_check.png` script); F7: real-TEP coverage-by-regime grouped bars | 010 | 🟡 F6 ✅ script exists; F7 script missing (numbers in results §1D.1b) |
| C8 | Bias-update + ACI jointly deliver calibrated intervals under label delay (θ-robust) | Coverage stable across θ∈{2,5} on TEP (0.90±0.003) and SECOM (0.910/0.915) | same as C7 + `scripts/secom_baseline.py` | T2: coverage table θ×method×dataset | 010, 012 | ✅ (table) |
| C9 | Conformal validity is model-agnostic: nominal coverage survives a failed point model, with narrower intervals than the static baseline | SECOM: R² −1.84 yet ACI cov 0.910/0.915 @ width 12.8 vs split 0.953 @ width 20.2 (−37% width) (results §1E) | `scripts/secom_baseline.py` | T2 + F8: SECOM band plot segment | 012 | 🟡 F8 script missing (optional; T2 may carry it) |
| C10 | Physics anchors are the *accuracy* difference — the no-physics negative control fails exactly where the physics benchmarks succeed | Same pipeline/protocol: TEP/Debutanizer R² positive; SECOM R² < 0 even corrected (results §1A vs §1E) | cross-cutting | T3: one-row-per-dataset summary table | 007, 012 | ✅ (table) |
| C11 | The framework deploys: stateful two-flow serving honoring the delayed-label/stored-interval invariant, with container + CI + live monitor | 67 unit tests; CI green (lint+unit+container smoke); local docker build 60 s; dashboard demo (results §1D.2/1D.3) | `serving/*`, `Dockerfile`, `.github/workflows/ci.yml` | F1: architecture diagram | 011 | 🟡 F1 diagram to draw; latency MEASURED & FROZEN: single-row /predict p50 1.23 ms / p90 1.51 / p99 1.97 (n=10⁴); batch-32 p50 1.46 ms (≈46 µs/row); /label p50 0.97 ms — client-observed incl. HTTP (`serving_latency.json`) |
| C12 | Fail-enrichment of conformal misses on SECOM | miss-rate fail 0.111/0.000 vs pass 0.089, **n=9** | `scripts/secom_baseline.py` | — | 012 | 🔴 underpowered — report only as "inconclusive (n=9)", never as a finding |

## Cross-cutting gaps (must clear before submission)
1. **Figure scripts** — ✅ F3/F4/F5/F7 frozen + rendered via `scripts/paper_figures/make_all.py`; F6 script pre-existing. REMAINING: F2 (Debutanizer ablation runner) + F1 (architecture diagram). Note: F3's Debutanizer α-path panel was cut — no such path ever existed (the 1A lottery is fold-spread evidence, carried by F4 + tables).
2. **Latency claim (C11)** — ✅ RESOLVED: `scripts/bench_latency.py` measured and froze p50/p90/p99 (see C11 row).
3. **Debutanizer dataset citation** — Fortuna et al. 2007 (*Soft Sensors for Monitoring and Control of Industrial Processes*, Springer) is not in the project PDF set; needed for the reference list (no implementation dependency).
4. **Nonlinear-baseline objection** — anticipated review ask ("would XGBoost fix SECOM?"). Position in Discussion: linear-first scope is deliberate (interpretability + the negative-control logic requires holding model class fixed); nonlinear source models are the documented extension (1D.5 gated, ADR-012 revisit trigger). Do **not** run a rushed nonlinear arm now.
5. **Reproducibility statement** — repo is private; decide public release vs "available on request" at submission.
