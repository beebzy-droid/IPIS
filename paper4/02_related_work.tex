\section{Related work}
\label{sec:related}

\paragraph{Conformal prediction beyond exchangeability.}
These methods give distribution-free, finite-sample coverage under the single
assumption that calibration and test data are exchangeable~\citep{vovk2005,shafer2008,
angelopoulos2023}, and regression variants such as conformalized quantile
regression~\citep{romano2019} and split-conformal methods~\citep{lei2018} make the
intervals locally adaptive. Exchangeability is the assumption that fails in feedback.
Two lines of work relax it. \citet{tibshirani2019} reweight the calibration scores when
the covariate shift is known, while \citet{barber2023} bound the coverage loss under an
arbitrary shift by a total-variation-type distance between the calibration and test
distributions. The per-module penalty $2L_j\|\Delta\psi_{j,k}\|$ used here is a
physically computable instance of that bound, with the abstract distributional distance
replaced by a similitude departure that is known in closed form from the operating
point. Adaptive conformal inference~\citep{gibbs2021} takes a complementary route and
guarantees long-run coverage online under arbitrary dependence by adjusting the
conformal quantile after each observation; we return to it for the multi-cycle horizon
in the discussion.

\paragraph{Soft sensing with calibrated uncertainty.}
Inferential (soft) sensors are standard practice for online estimation of qualities
that resist direct measurement~\citep{fortuna2007}, and the debutanizer is among the
most studied benchmarks, including recent machine-learning soft sensors for
product-quality monitoring on cyber-physical columns~\citep{rozanec2022}. The
constituent soft-sensor module~\citep{busico_m1} adds physics-anchored features and an
adaptive conformal calibration that holds coverage under delayed labels. We consume its
interval as the quality guarantee in the composition.

\paragraph{Data-driven prognostics.}
Statistical and data-driven estimation of remaining useful life is surveyed
in~\citet{si2011} and, for rotating machinery, in~\citet{lei2018machinery}. The
constituent prognostic module~\citep{busico_m2} contributes a similarity-calibrated
conformal lower bound on remaining life, which we consume as the equipment-life
guarantee.

\paragraph{Real-time optimization and health-aware control.}
RTO adjusts setpoints to track the economic optimum despite plant-model mismatch.
Modifier adaptation~\citep{marchetti2009,chachuat2009} corrects the optimizer with
measured plant gradients and is the method the constituent RTO
module~\citep{busico_m3} builds on. A broader lineage embeds economic objectives
directly in the feedback layer~\citep{skogestad2000}, and health- or constraint-aware
variants restrict operation to protect equipment. What this body of work does not yet
offer is a distribution-free coverage guarantee on the joint quality-and-life event that
\emph{composes} the uncertainty of the upstream estimators. The optimizer is made
health-aware, but the soft-sensor and prognostic guarantees are never propagated into a
certificate on the closed-loop outcome.

\paragraph{The gap.}
Across these literatures the per-layer guarantees exist and the health-aware optimizer
exists, yet their composition under feedback has not been addressed: a single per-cycle
certificate on the system-safety event, derived rather than asserted and enforceable as
one optimizer constraint. Supplying that composition is the contribution of this paper.
